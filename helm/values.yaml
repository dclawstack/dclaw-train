replicaCount: 1
image:
  backend:
    repository: dclaw-train-backend
    tag: local-1779987710
    pullPolicy: Never
  frontend:
    repository: dclaw-train-frontend
    tag: local-1779987710
    pullPolicy: Never
service:
  backend:
    type: ClusterIP
    port: 8039
  frontend:
    type: NodePort
    port: 3039
    nodePort: 30072
ingress:
  enabled: false
secret:
  data:
    DATABASE_URL: "postgresql+asyncpg://postgres:postgres@dclaw-shared-postgres:5432/dclaw_train"
    APP_ENV: "production"
    SECRET_KEY: "dev-secret-change-me"
    OLLAMA_URL: "http://ollama:11434"
    OPENROUTER_API_KEY: ""
    NEXT_PUBLIC_API_URL: ""
    ENABLE_DEMO_MODE: "true"
resources:
  requests: { cpu: 10m, memory: 128Mi }
  limits:   { cpu: 300m, memory: 384Mi }
strategy:
  type: Recreate
